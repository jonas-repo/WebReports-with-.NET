@model IEnumerable<ReportesWeb.Models.Tipo_falla>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_falla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_falla)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_falla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_falla)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_tipo_fallaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_tipo_fallaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_tipo_fallaID })
        </td>
    </tr>
}

</table>
